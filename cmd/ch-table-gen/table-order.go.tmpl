ORDER BY (
    `index_type`,
    `metric`,
    `pre_tag`,
    `pre_stag`,
    `time`,
	{{range $i := .RawTags }}
    `rtag{{$i}}`,
	{{- end}}
	{{range $i := .BasicTags }}
    `tag{{$i}}`, 
    `stag{{$i}}`,
	{{- end}}
    `raw_flags`
)
